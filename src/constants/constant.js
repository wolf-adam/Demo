const Status = {
    'START': 'start',
    'INSTRUCTIONS': 'instructions',
    'NEW': 'new',
    'RESUME': 'resume',
    'END': 'end'
}

const Result = {
    "LOSE": false,
    "WIN": true,
}

const MAX_TRIES = 10;

export { Status, Result, MAX_TRIES }
